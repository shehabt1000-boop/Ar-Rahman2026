name: Send Notifications

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  send:
    runs-on: ubuntu-latest

    steps:
      - name: Call Notification API
        run: |
          curl -X GET https://ar-rahman-seven.vercel.app/api/send-notification
